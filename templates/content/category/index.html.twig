{% extends 'base.html.twig' %}

{% block title %}Category index{% endblock %}

{% block body %}
    <h1>Category index</h1>

    {# <table class="table"> #}
        {# <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody> #}
        {% for category in category %}
        <div class="student">
            <tr>
                <p>{{ category.id }}</p>
                <p id="promo">{{ category.name }}</p>
                <p id="name">{{ category.description }}</p>
                <td>
                    <a href="{{ path('app_category_show', {'id': category.id}) }}">show</a>
                    <a href="{{ path('app_category_edit', {'id': category.id}) }}">edit</a>
                    <a href="{{ path('app_category_delete', {'id': category.id}) }}" onclick="return confirm('Are you sure?')">delete</a>
                </td>
            </tr>
                        </div>

        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        {# </tbody> #}
    {# </table> #}

    <a href="{{ path('app_category_new') }}">Nouvelle Création</a>
{% endblock %}
